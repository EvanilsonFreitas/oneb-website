# contexts

React Contexts globais (ex: tema, preferências de UI). Usar apenas quando o estado precisar ser compartilhado por múltiplas árvores de componentes distantes — preferir estado local sempre que possível.
