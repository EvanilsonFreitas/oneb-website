# types

Tipos e interfaces TypeScript compartilhados entre múltiplas features (ex: modelos de domínio retornados pelos services). Tipos usados por uma única feature devem viver dentro da própria feature.
