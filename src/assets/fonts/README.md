# assets/fonts

Arquivos de fonte auto-hospedados que não são distribuídos via pacote npm (ex: variações customizadas da Plus Jakarta Sans). A fonte oficial já é carregada via `@fontsource/plus-jakarta-sans`.
