# components/ui

Base de componentes gerada e mantida via shadcn/ui (`npx shadcn add`). Serve como fundação para o Design System OneB — não deve ser usada diretamente pelas páginas sem passar por adaptação em `components/common` ou `components/sections` quando a identidade visual exigir customização.
